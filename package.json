{
 "private": true,
 "workspaces": [
  "packages/*"
 ],
 "devDependencies": {
  "@changesets/cli": "^2.26.2",
  "turbo": "^1.10.16"
 },
 "scripts": {
  "dev": "turbo run dev --parallel",
  "build": "turbo run build",
  "changesets": "changesets",
  "version-packages": "changesets version",
  "release": "turbo run build --filter=@davicecilio-ui/docs && changeset publish"
 }
}
